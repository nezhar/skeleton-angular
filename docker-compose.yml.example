version: '2'
services:
  # Node configuration
  node:
    build:
      context: ./docker/node
    volumes:
      - ./:/app
    command: ng serve --host 0.0.0.0 --port 8080
    ports:
      - "8080:8080" # ng serve
      - "9876:9876" # storybook?
      - "9001:9001" # storybook?
