import { AuthState } from '@app/shared/state/auth/auth.state';
import { CountState } from '@app/shared/state/count/count.state';
import { LanguageState } from '@app/shared/state/language/language.state';

export const states = [
    AuthState,
    CountState,
    LanguageState,
];
